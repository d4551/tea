/**
 * LOTFK Game Contracts — shared canonical entrypoint.
 *
 * Re-export the production contract source used by server and client bundles
 * to keep API payload types and runtime contracts single-sourced.
 */
export * from "../../src/shared/contracts/game.ts";

/**
 * Canonical AI-related constants.
 * Single source of truth for model context lengths and related values.
 */

/** Default context length for Ollama models when parameter size cannot be parsed (tokens). */
export const OLLAMA_DEFAULT_CONTEXT_LENGTH = 4096;

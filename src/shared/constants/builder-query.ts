/**
 * Canonical builder query parameter keys.
 */

/** Query parameter used for paginated builder library views. */
export const BUILDER_QUERY_PARAM_PAGE = "page";

/** Query parameter used to keep the active scene in URL state. */
export const BUILDER_QUERY_PARAM_SCENE_ID = "sceneId";

/** Query parameter used to keep the active asset in URL state. */
export const BUILDER_QUERY_PARAM_ASSET_ID = "assetId";
